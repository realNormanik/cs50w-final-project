[project]
name = "leadflow"
version = "1.0.0"
description = ""
requires-python = ">=3.12"
readme = "README.md"
dependencies = [
    "django-cf",
    "django==5.2.6",
    "tzdata",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk"
]
