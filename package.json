{
  "name": "leadflow",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dependencies": "uv pip install --system workers-py --upgrade && uv run pywrangler sync",
    "deploy": "pnpm dependencies && uv run pywrangler deploy",
    "dev": "uv run pywrangler dev",
    "start": "uv run pywrangler dev",
    "db": "wrangler d1 execute cs50 --file=./database/schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.120.1"
  },
  "packageManager": "pnpm@11.21.0"
}
